{
  "name": "@rotirojs/api-demo",
  "version": "1.0.0",
  "description": "Rotiro Api Demo",
  "author": "Alex Everitt",
  "scripts": {
    "clear-build": "rm -rf lib/*",
    "build": "rm -rf lib/* && tsc",
    "prepublishOnly": "rm -rf lib/* && tsc",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "lint-fix": "tslint -c tslint.json 'src/**/*.ts' --fix"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/rotirojs/api-demo.git"
  },
  "license": "MIT",
  "types": "lib/index.d.ts",
  "main": "lib/index.js",
  "devDependencies": {
    "@types/node": "^14.14.5",
    "prettier": "^1.19.1",
    "tslint": "^5.20.0",
    "tslint-config-prettier": "^1.18.0",
    "typescript": "^4.0.5"
  },
  "dependencies": {
    "rotiro": "^1.0.29"
  },
  "directories": {
    "lib": "lib"
  }
}
